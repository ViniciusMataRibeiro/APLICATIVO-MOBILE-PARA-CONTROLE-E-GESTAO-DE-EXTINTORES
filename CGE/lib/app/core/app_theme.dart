import 'package:flutter/material.dart';

var colorScheme = ColorScheme.fromSeed(seedColor: const Color.fromARGB(255, 116, 7, 7));

final ThemeData themeData =
    ThemeData(colorScheme: colorScheme, useMaterial3: true);
